# SalesDuo Frontend

Minimal React + Vite + TypeScript frontend.

## Quick start

```bash
cd frontend
npm install
npm run dev
```

The app expects the backend to be available at `http://localhost:4000`.
You can change this via the `VITE_API_BASE_URL` variable in `.env`.